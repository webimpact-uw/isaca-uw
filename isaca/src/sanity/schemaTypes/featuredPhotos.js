import {defineField, defineType} from 'sanity';

export default {
  name: 'featuredPhotos',
  title: 'Featured Photos',
  type: 'object',
  fields: [
    defineField({

    }),
    defineField({

    })
  ]
}